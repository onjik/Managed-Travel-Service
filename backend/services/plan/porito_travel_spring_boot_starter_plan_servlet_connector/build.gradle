bootJar {enabled = false}
jar {enabled = true}

dependencies {
    implementation project(':services:plan:plan_domain')
    implementation project(':common:porito_travel_spring_boot_starter_servlet_connector')

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}